git help log
man git-log

Initialize a git project all tracking under the directory
git init
create folder  : myproject
cd to the folder : myproject/git init

create text in the file
first.txt

add all changes to the directory 
git add .
git add filename

commit changes to the repository with message
git commit -m "inittail commit"

git log -n 1 //limits the number of commits
git log --since=2012
git log --unitl=2012-12-4
git log --author="fikru"
git log --grep="Init" regular expression


Two-tree architecture
	      ------------>checkout	
Repository    ------- - -- - Working
             <-----------comimit

Git uses three-tree architecture
working - -- staging index   --- Repositor
git add file.txt ------------> git commit file.txt


work flow
1st commit
new file (V1)---V1----V1

eddited file
v2 ----

git diff compares what is in repository (-) and working area(++)

git fiff
git diff filename

git diff --staged compares to the repository
git diff --cached

delete file and track
git add .
git commit -m "add for deletion"

delete from the file directory
git rm file_to_delete rm ""file_to_delete
this puts delection in staging
git commit -m "remove second red shirt"
